---

spring:
  profiles:
    active: development
  jpa:
    generate-ddl: true
  output:
    ansi:
      enabled: always
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs